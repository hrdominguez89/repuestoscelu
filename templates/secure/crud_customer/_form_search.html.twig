{{ form_start(form, {'attr': {'id': 'customer_search'}}) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>

    <div class="row">
        <div class="col">
            {{ form_row(form.name) }}
        </div>       
        <div class="col-2">
            <div class="mb-3">
                <label class="form-label" for="customer_search">&nbsp;</label>
                <button class="btn btn-primary form-control">Buscar</button>
            </div>
        </div>
    </div>
{{ form_end(form) }}