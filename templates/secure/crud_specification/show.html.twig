{% extends 'home/index.html.twig' %}

{% block title %}Especificación{% endblock %}

{% block content %}
    <h1>Especidicación {{ specification.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ApiId</th>
                <td>{{ specification.apiId }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ specification.slug }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-inline-flex">
    <a style="width: 80px" class="btn btn-secondary m-1" href="{{ path('secure_crud_specification_index') }}">Regresar</a>

    <a style="width: 80px" class="btn btn-primary m-1"  href="{{ path('secure_crud_specification_edit', {'id': specification.id}) }}">Editar</a>

    {{ include('secure/crud_specification/_delete_form.html.twig') }}
</div>
{% endblock %}
