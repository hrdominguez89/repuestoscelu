{{ form_start(form) }}
    {{ form_widget(form) }}
<button style="width: 80px" class="btn btn-primary">Guardar</button>
<a style="width: 80px" class="btn btn-secondary" href="{{ path('secure_crud_specification_index') }}">Regresar</a>
{{ form_end(form) }}
